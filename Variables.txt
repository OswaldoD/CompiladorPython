#Esta es una prueba para las declaraciones de variables globales y locales y listas 



def funcion2(a, b) :
	float a, b, c
	boolean g k # error por la coma


	a= a*e
;

def enlist(lista):
    if 0 in lista: #Probar el in
        return lista
    ;
    else:
        return 'No esta en la lista'
    ;
;

int g, y, x
float a, b, c
boolean g k # error por la coma
char z, t, y, y 
lis=[ 1,2,3]
lista = [ 3, 'Hola', 6 7] # Error por la coma

lista = [ 3, 5, 6 ,7  # Error por la parentesis

char z, t, y, y 


y      

int k   # da error por la linea anterior


#Programa

a = a * b

