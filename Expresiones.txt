#Esta es una prueba para probar las expresiones que son aceptadas por el parser


def funcion1(x):

#Todas las siguientes expresiones son aceptadas,

  x+= 4 
  x = t(8) // a % g
  a = a ** c
  x=2

   4 = g + t # Error
  funcion1(x==3) #se espera que acepte llamado a funciones
  funcion1(3+x, 3=z) #tambien se pueden incluir esto en parametros 
  a =  2 % 3 and 345 +8*9+5-3) #se aceptan combinaciones 

;

#Programa


   a a * c + 55 - funcion(8) # Error no tiene asignacion
   x = t(8) // a % g
   4 = x   # Error
   a = a ** (c - // h # combinacion de operadores





